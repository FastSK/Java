/*
package Collection.Queue_Sort;

import java.util.*;

class Main {
    public static void main(String[] args) {
        Queue<Integer> queue = new LinkedList<>();

        // Asking for the input
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter number: (press 'x' to exit)");

        // Scan the input and add it to stack
        while (sc.hasNextInt() && !sc.hasNext("x")) {
            System.out.println("Enter number: (press 'x' to exit)");
            int number = sc.nextInt();

            // add the integer to the queue
            queue.add(number);
            System.out.println("List before sorting: " + queue);
        }

        // sort the queue using collection framework
        Collections.sort();
        System.out.println(queue);
    }
}

*/
