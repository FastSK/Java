package Sort;


/**
 * Selection Sort
 * repeatedly selecting the largest(or smallest) element from unsorted portion of the list
 * Move it to the sorted portion of the list
 */
public class SelectionSort {
    void selectionSort(int[] arr){
        int n = arr.length;

        //
        for (int i = 0; i < n-1; i++) {

        }
    }
}
